# 个人音乐电台网站 - 技术规范文档

## 1. 项目概述

### 1.1 核心定位
混合模式音乐电台：策展频道（管理员创建）+ 个性化推荐（基于网易云 API）并存。

### 1.2 目标用户
个人/朋友规模（<100 人），PC 优先设计，仅中文界面。

### 1.3 技术栈
- **框架**: Next.js (App Router) + React 19
- **样式**: Tailwind CSS v4 + Framer Motion（高级粉奶油手账风、3D视差）
- **状态管理**: React Context + useReducer
- **部署**: Vercel（零运维、自动部署）
- **数据存储**: 本地 SQLite 数据库 (Drizzle ORM) + localStorage

---

## 2. 核心功能模块

### 2.1 音乐播放器
**功能要求：**
- 自定义播放器 UI（非 iframe 嵌入）
- 支持播放/暂停、上一首/下一首、进度条拖拽、音量调节
- 支持随机播放和顺序播放切换
- 底部固定播放条，始终可见
- 支持键盘快捷键：
  - 空格：播放/暂停
  - 左右方向键：快进/快退 10 秒
  - 上下方向键：音量调节
  - N 键：下一首
  - M 键：静音/取消静音

**技术实现：**
- 通过 Next.js API Routes 代理网易云 API 请求（隐藏 API Key、解决跨域）
- 使用 HTML5 `<audio>` 元素控制播放
- 播放状态全局共享（Context API）

### 2.2 策展频道系统
**频道分类：** 按场景分类（如「深夜放松」「工作专注」「运动节拍」「学习陪伴」）

**管理员功能：**
- 创建/编辑/删除频道
- 为频道添加/移除歌曲
- 管理频道封面、描述、标签

**用户投稿：**
- 用户输入网易云歌曲链接/ID + 投稿理由
- 管理员审核后加入频道
- 无注册体系，投稿时填写昵称+邮箱（可选）

### 2.3 个性化推荐电台
**实现方式：**
- 调用网易云官方推荐 API（需确认 API 权限）
- 基于用户播放历史和喜欢的歌曲生成推荐
- 推荐结果存入 localStorage，支持手动刷新

**推荐算法（轻量级）：**
- 记录用户播放的歌曲 ID 和喜欢的歌曲
- 调用网易云「相似歌曲」或「推荐歌单」API
- 去重后展示推荐结果

### 2.4 歌曲评论展示
**交互方式：** 实时展示网易云热门评论
- 播放歌曲时自动获取并展示热门评论
- 支持滚动浏览
- 评论区与播放器联动（切换歌曲时自动更新）

### 2.5 歌词展示
**功能要求：**
- 显示当前播放歌曲的歌词
- 支持滚动高亮（跟随播放进度）
- 无歌词时隐藏歌词区域

**技术实现：**
- 调用网易云歌词 API（需确认 API 权限）
- 解析 LRC 格式歌词
- 使用 Intersection Observer 实现滚动高亮

### 2.6 用户偏好系统
**数据存储（localStorage）：**
- 喜欢的歌曲列表
- 我的歌单（用户创建的歌单）
- 播放历史
- 播放设置（音量、播放模式等）

**数据管理：**
- 提供一键清除本地数据功能
- 数据导出/导入功能（可选）

---

## 3. UI/UX 设计规范

### 3.1 视觉风格
- **主题**: 三丽鸥水彩背景 / 音乐手账风
- **配色**: 高级粉奶油配色（背景 `#F5F1E6`、表面 `#FFFFFF`、强调色 `#D4858A`）
- **字体**: 中文使用系统默认字体，英文使用 DM Sans 或 Space Grotesk
- **圆角**: 8px-16px，柔和曲线
- **动画**: Framer Motion 驱动的页面切换、骨架屏微光、歌词物理弹簧动效与 3D 视差

### 3.2 页面布局
- **整体结构**: 全宽流式布局（Full-width fluid layout） + 音乐手账式结构
- **个性化电台**: 深度重构的电台布局，支持歌单收藏功能
- **内容区**: 沉浸式视差滚动，背景暗纹装饰
- **播放器**: 底部固定浮动播放器，统一控件悬浮样式

### 3.3 响应式设计
- **PC 优先**: 最小宽度 1024px
- **平板适配**: 768px-1024px，导航栏可折叠
- **移动端兼容**: <768px，底部 Tab 导航

### 3.4 动画效果
- **页面切换**: 淡入淡出过渡（200ms）
- **播放器展开**: 底部滑出动画
- **进度条**: 平滑过渡
- **按钮交互**: 悬停缩放效果
- **加载状态**: 骨架屏 + 霓虹色加载动画

---

## 4. 技术架构

### 4.1 项目结构
```
src/
├── app/                    # Next.js App Router 页面
│   ├── layout.tsx         # 全局布局
│   ├── page.tsx           # 首页
│   ├── channel/[id]/      # 频道详情页
│   ├── radio/             # 个性化推荐电台
│   ├── my/                # 我的音乐（喜欢、歌单、历史）
│   └── admin/             # 管理后台
├── components/            # 组件
│   ├── player/            # 播放器相关组件
│   ├── channel/           # 频道相关组件
│   ├── ui/                # 通用 UI 组件
│   └── layout/            # 布局组件
├── hooks/                 # 自定义 Hooks
├── lib/                   # 工具函数
│   ├── api.ts             # 网易云 API 封装
│   ├── storage.ts         # localStorage 操作
│   └── utils.ts           # 通用工具
├── contexts/              # React Context
│   └── PlayerContext.tsx  # 播放器状态
├── types/                 # TypeScript 类型定义
└── styles/                # 全局样式
```

### 4.2 API 代理层
**Next.js API Routes 代理：**
```typescript
// app/api/netease/[...path]/route.ts
// 代理所有网易云 API 请求
// - 隐藏 API Key
// - 处理跨域
// - 添加请求限流
// - 错误处理和重试
```

**API 限流策略：**
- 前端展示限流提示
- 提供手动重试按钮
- 不自动重试（避免雪崩）

### 4.3 状态管理
**PlayerContext 全局状态：**
```typescript
interface PlayerState {
  currentSong: Song | null;
  isPlaying: boolean;
  volume: number;
  progress: number;
  duration: number;
  playMode: 'sequential' | 'random';
  playlist: Song[];
  currentIndex: number;
}
```

### 4.4 数据流
1. **频道数据**: 管理员通过 API 创建 → 存储在网易云歌单 → 前端请求展示
2. **用户数据**: 播放/喜欢操作 → 存储在 localStorage → 推荐算法读取
3. **播放控制**: 用户操作 → 更新 PlayerContext → 触发 audio 元素

---

## 5. 边界情况与风险

### 5.1 API 相关风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 网易云 API 限流 | 无法获取歌曲/评论 | 友好提示 + 手动重试 |
| API 接口变更 | 功能失效 | API 代理层封装，便于修改 |
| 歌曲下架/版权限制 | 无法播放 | 展示提示信息，跳过该歌曲 |
| 推荐 API 不可用 | 无法生成推荐 | 降级为随机播放策展频道 |

### 5.2 用户体验风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| localStorage 数据丢失 | 用户偏好丢失 | 提供数据导出功能 |
| 跨设备不同步 | 数据不一致 | 提示用户数据仅本地存储 |
| 播放器在后台被浏览器暂停 | 播放中断 | PWA 支持 + 提示用户 |
| 移动端兼容性问题 | 部分功能不可用 | 优雅降级，核心功能可用 |

### 5.3 性能风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 大量歌曲加载慢 | 首屏加载时间长 | 虚拟滚动 + 懒加载 |
| 频繁 API 调用 | 触发限流 | 本地缓存已请求数据 |
| localStorage 数据过大 | 性能下降 | 定期清理历史数据 |

---

## 6. 开发规范

### 6.1 代码规范
- **格式化**: ESLint + Prettier 统一代码风格
- **命名规范**: 
  - 组件: PascalCase (如 `PlayerBar`)
  - 函数/变量: camelCase (如 `playSong`)
  - 文件: kebab-case (如 `player-bar.tsx`)
- **类型安全**: 严格 TypeScript，避免 `any`

### 6.2 项目结构规范
- **组件**: 按功能模块组织，每个组件单独文件夹
- **Hooks**: 按功能命名，如 `usePlayer`、`useLocalStorage`
- **工具函数**: 按职责分类，避免重复代码

### 6.3 Git 规范
**提交信息格式（Conventional Commits）：**
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型：**
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**分支策略：**
- `main`: 生产分支
- `develop`: 开发分支
- `feature/*`: 功能分支
- `fix/*`: 修复分支

### 6.4 单元测试
- **测试框架**: Jest + React Testing Library
- **覆盖范围**: 
  - 工具函数（100% 覆盖）
  - 自定义 Hooks
  - 关键业务逻辑
- **测试文件**: 与源文件同目录，命名 `*.test.tsx`

---

## 7. 部署与运维

### 7.1 部署环境
- **平台**: Vercel
- **自动部署**: Push 到 `main` 分支自动部署
- **预览部署**: PR 自动创建预览环境

### 7.2 环境变量
```env
# .env.local
NETEASE_API_KEY=xxx          # 网易云 API Key（仅服务端使用）
NETEASE_API_SECRET=xxx       # 网易云 API Secret
```

### 7.3 监控与日志
- **错误监控**: Vercel Analytics + 自定义错误边界
- **性能监控**: Core Web Vitals 追踪
- **日志**: API 调用日志（仅错误日志）

---

## 8. 待确认事项

### 8.1 API 权限确认
- [ ] 网易云个人开发者 API 具体可用接口列表
- [ ] 推荐 API 是否可用（相似歌曲、推荐歌单）
- [ ] 歌词 API 是否可用
- [ ] 热门评论 API 是否可用
- [ ] 播放链接 API 是否可用

### 8.2 设计细节
- [ ] 具体配色方案（霓虹色具体色值）
- [ ] 字体选择（中英文）
- [ ] 图标库选择（Lucide Icons 推荐）
- [ ] 具体动画参数（时长、缓动函数）

### 8.3 功能优先级
- [ ] MVP 功能范围确认
- [ ] 后续迭代功能规划

---

## 9. 开发计划建议

### Phase 1: 基础框架（1-2 周）
- 项目初始化（Next.js + TypeScript + Tailwind）
- 布局结构（侧边栏 + 内容区 + 播放器）
- API 代理层搭建
- 播放器核心功能

### Phase 2: 频道系统（1-2 周）
- 频道列表展示
- 频道详情页
- 管理员后台（创建/编辑频道）

### Phase 3: 用户功能（1 周）
- 喜欢/收藏功能
- 我的歌单
- 播放历史
- 数据清理功能

### Phase 4: 个性化推荐（1 周）
- 推荐电台页面
- 推荐算法实现
- 歌词展示

### Phase 5: 优化与测试（1 周）
- 错误处理完善
- 性能优化
- 单元测试编写
- 响应式适配

---

## 10. 附录

### 10.1 技术选型对比
| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| Next.js + React | 生态成熟、API Routes 代理 | 学习曲线 | ✅ 采用 |
| Vercel 部署 | 零运维、自动部署 | 国内访问较慢 | ✅ 采用 |
| SQLite + Drizzle | 持久化强，类型安全 | 相比纯本地稍重 | ✅ 采用 |
| 自定义播放器 | 完全控制 UI | 工作量大 | ✅ 采用 |

### 10.2 参考资源
- [Next.js 文档](https://nextjs.org/docs)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [网易云音乐开放平台](https://open.music.163.com)
- [PWA 文档](https://web.dev/articles/progressive-web-apps)
